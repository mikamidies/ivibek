# 🔍 Отладка системы аутентификации

## Что было исправлено

### ❌ Проблемы до исправления:

1. **Разлогин при любой ошибке сети** - если сервер был недоступен на момент обновления токена, пользователя сразу разлогинивало
2. **Превентивное обновление каждые 14 минут** - при временной сетевой ошибке = logout
3. **Нет различия между невалидным токеном и сетевой ошибкой**

### ✅ Что исправлено:

1. **Умная обработка ошибок в `refresh()`**:

   - Разлогиниваем только при 401/403 (токен невалидный)
   - При сетевых ошибках (таймаут, нет интернета) - токены НЕ сбрасываем

2. **Улучшенное логирование**:

   - Все действия теперь логируются с эмодзи для удобства
   - `console.trace()` при logout покажет откуда вызван

3. **Добавлена функция `safeFetch()`**:

   - Автоматически добавляет токен к запросам
   - При 401 автоматически обновляет токен и повторяет запрос
   - Можно использовать в других composables

4. **Счетчик попыток в превентивном обновлении**:
   - Отслеживает неудачные попытки
   - Не паникует при первой же ошибке

## 📊 Как отследить проблему

### 1. Откройте Console в браузере (F12)

Теперь вы увидите:

```
⏰ Превентивное обновление токена (каждые 14 минут)
✅ Токены успешно обновлены
```

При ошибках:

```
❌ Ошибка обновления токенов: [детали ошибки]
🔒 RefreshToken невалидный - разлогиниваемся  // только при 401/403
⚠️ Временная ошибка сети - токены НЕ сброшены  // при других ошибках
```

При logout:

```
🚪 Выполняется logout
Logout вызван из:
  at logout (auth.ts:45)
  at refresh (auth.ts:125)
  ...
```

### 2. Проверьте Network tab

Смотрите запросы к:

- `/api/v1/student/auth/refresh` - обновление токенов
- `/api/v1/student/profile` - загрузка профиля

Обратите внимание на:

- Статус ответа (200, 401, 500, etc.)
- Время ответа (таймауты?)
- Response body (что возвращает сервер)

### 3. Проверьте Application → Cookies

Токены должны быть:

- `access_token` - живет 15 минут
- `refresh_token` - живет 30 дней

## 🐛 Возможные причины разлогина

### 1. RefreshToken истек на сервере

**Симптом**: Logout через строго 30 дней  
**Решение**: Это нормально, нужно заново войти

### 2. Сервер возвращает 401 при refresh

**Симптом**: В консоли `🔒 RefreshToken невалидный - разлогиниваемся`  
**Причина**: Сервер не принимает refreshToken  
**Проверить**:

- Правильно ли отправляется refreshToken в body
- Что сервер возвращает в ответе

### 3. Cookies не сохраняются

**Симптом**: После перезагрузки страницы - logout  
**Причина**: Проблемы с cookies (SameSite, Secure, Domain)  
**Решение**: Проверить настройки useCookie

### 4. Сервер нестабилен

**Симптом**: Случайные logout'ы  
**Причина**: API возвращает ошибки  
**Проверить**: Логи сервера, доступность API

## 🔧 Дополнительные улучшения

### Можно добавить:

1. **Retry логику** - повторять failed запросы несколько раз
2. **Offline detection** - не пытаться обновлять токены без интернета
3. **Toast уведомления** - показывать пользователю "Потеряно соединение"
4. **Продление refreshToken** - некоторые API позволяют обновлять и refresh токен

## 📝 Что смотреть в консоли

Если видите частые logout, ищите паттерн:

```
⏰ Превентивное обновление токена
❌ Ошибка обновления токенов: [ОШИБКА]
```

Запишите:

- Время между logout
- Текст ошибки
- Статус код (если есть)
- Какие действия делали перед этим

Это поможет найти корень проблемы.
